request包安装:
    1、pip install requests（dos窗口需要先cd到python下的Scripts目录,pip在这个目录下面）
    2、https://pypi.python.org/pypi/requests

框架实现:
    数据存储（cogfigparser,[xlrd]）
    封装方法（requests这个是接口,selenium这个是web）
    创建执行任务，执行任务下的testCase（unittest）,数据驱动ddt，从configparser中获取数据
    断言（jdbc，正则表达式？字典？）
    生成测试报告（HTMLTestRunner，HTMLTestRunner是第三方开源包，得网上找资源）
    [发送email测试报告]（这个我不会写，看情况，网上找代码copy）
    失败任务，定位失败问题（log日志，自己写）